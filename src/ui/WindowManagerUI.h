// ============================================================================
//  WaylandCraft-BE — ui/WindowManagerUI.h
//  Port of upstream gui/WindowManagerScreen (key B): per-window actions
//  Grab / Resize / Hide / Pin / Give Window Item / Help / Capture Mode.
// ============================================================================
#pragma once

namespace wlc {

class WindowManagerUI {
public:
    static void toggle();
};

} // namespace wlc
