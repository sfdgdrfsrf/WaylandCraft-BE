// WaylandCraft-BE capture-helper companion app.
// Streams an Android app's window into the in-game compositor.
//
// Build: `gradle :app:assembleRelease` inside a standard Android Studio
// project containing these sources (minSdk 28, targetSdk 34, no deps).
rootProject.name = "waylandcraft-capture-helper"
include(":app")
